/** Default dashboard role when API omits role (maps to student shell). */
export const ROLES = {
  STUDENT: 'student',
  LECTURER: 'lecturer',
  ADMIN: 'admin',
}

export const STORAGE_THEME = 'edusense-theme'
export const STORAGE_ACCESS = 'edusense-access-token'
/** Mock profile JSON when API URL unset / VITE_USE_MOCK */
export const STORAGE_MOCK_PROFILE = 'edusense-mock-profile'
